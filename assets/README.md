# assets/

可複用的模板和 checklist。

| 路徑 | 用途 |
|------|------|
| `decision-tree.md` | 工具路由決策樹（什麼任務用什麼工具） |
| `handoff-templates/` | 各階段產出的 Markdown 模板（在 `_drafts/`） |
| `checklists/` | 實作前、部署前的檢查清單（在 `_drafts/`） |

## 啟用狀態

目前啟用：
- `decision-tree.md`

未啟用（在 `_drafts/assets/`）：handoff-templates、checklists。
