---
description: ai/workflow-state.md 出現時遵循的五階段自主開發流程（ANALYZE → BLUEPRINT → CONSTRUCT → VALIDATE → DELIVER）
globs: "**/ai/workflow-state.md"
alwaysApply: false
---

# 自主開發迴圈

## 觸發與適用

當對話 context 含 `ai/workflow-state.md` 時啟用本規則。
這是一份「動態大腦」文件，AI 必須在每個階段轉換時讀寫它。

## 核心原則

1. **狀態驅動** — 每次行動前先讀 `workflow-state.md` 的 State 區塊，操作後更新
2. **強制停下點** — Status 為 `NEEDS_PLAN_APPROVAL`、`NEEDS_HUMAN_REVIEW`、`BLOCKED` 時必須停下回報，等用戶確認
3. **可退回** — CONSTRUCT 中發現 BLUEPRINT 有錯，必須退回 BLUEPRINT 並在 Log 標記原因，禁止擅自改 Plan 繼續
4. **失敗計數** — VALIDATE 失敗即遞增 `ValidateAttempts`，達 3 強制 BLOCKED
5. **證據優先** — 每次狀態變更需附驗證證據（檔案 + 行數 / build / test 結果），對齊 `workflow-completion-evidence.mdc`

## 狀態機

合法 Phase：`ANALYZE` | `BLUEPRINT` | `CONSTRUCT` | `VALIDATE` | `DELIVER` | `BLOCKED`

合法 Status：`IN_PROGRESS` | `NEEDS_PLAN_APPROVAL` | `NEEDS_HUMAN_REVIEW` | `BLOCKED` | `ABORTED`

> **注意**：INIT **不是** Phase 值。它是進入狀態機之前的 preflight 偵測步驟（見下方「INIT」段）。
> Phase 欄位的初始值就是 `ANALYZE`，永遠不會被寫成 `INIT`。
> Log 區塊可使用 `INIT → ANALYZE` 作為人類可讀的事件標記，但這不是狀態轉換。

允許的轉換（Phase 之間）：

| 從 | 到 | 條件 |
|---|---|---|
| ANALYZE | BLUEPRINT | 任務理解完成 |
| BLUEPRINT | （停下）| 計畫產出 → 設 Status=NEEDS_PLAN_APPROVAL |
| BLUEPRINT | CONSTRUCT | 用戶口頭確認計畫 |
| CONSTRUCT | VALIDATE | 所有 Plan 項目完成 |
| CONSTRUCT | BLUEPRINT | 發現計畫有錯（必須在 Log 標記原因） |
| VALIDATE | CONSTRUCT | 失敗且 ValidateAttempts < 3 |
| VALIDATE | BLOCKED | 失敗且 ValidateAttempts = 3 |
| VALIDATE | DELIVER | 通過 |
| DELIVER | （停下）| Review 完成 → 設 Status=NEEDS_HUMAN_REVIEW |
| 任何 | ABORTED | 用戶手動中斷 |

任何不在表上的 Phase 轉換**禁止**。

## 各階段行為

### INIT（preflight 偵測，**不是** Phase）

這是進入狀態機**之前**的偵測步驟。Phase 欄位不會變成 INIT，初始值維持 `ANALYZE`。
INIT 的存在只是為了確保進入 ANALYZE 時 `CurrentTask` 已就緒。

檢查 `workflow-state.md`：

- 若 `CurrentTask` 為 `<unset>` 或空白：
  1. **不要自己編造任務**
  2. 主動詢問用戶：「請告訴我這次的任務是什麼？」
  3. 收到用戶回答後，**自動寫回 `workflow-state.md`**：
     - 把 `CurrentTask:` 改成用戶描述（一句話濃縮，保留關鍵資訊）
     - 把 `StartedAt:` 填入當前時間（格式 `YYYY-MM-DD HH:MM`）
  4. 在 Log 區塊 append：`[time] INIT → ANALYZE | 任務：<摘要>`（人類可讀的事件標記，不是 Phase 轉換）
  5. 然後進入 ANALYZE 階段（Phase 維持 `ANALYZE`，本來就是初始值）
- 若 `CurrentTask` 已填寫（非 `<unset>`）：
  - 直接進入 ANALYZE，不重複詢問

### ANALYZE（理解）

- **讀**：`workflow-state.md` + CurrentTask 描述 + 相關 spec / 程式碼
- **產**：對任務的理解摘要（口頭回報，不寫檔）
- **限**：不修改任何檔案（INIT 階段已完成的寫入除外）
- **下一步**：自動進 BLUEPRINT

### BLUEPRINT（規劃）

- **產**：可勾選的 checklist 寫入 `workflow-state.md` 的 Plan 區塊
- **要求**：
  - 每項要可獨立驗證（不能寫「優化效能」這種模糊條目）
  - 標記依賴順序（A → B 表示 B 依賴 A）
  - 識別風險與需用戶決策的點
- **完成**：設 `Phase=BLUEPRINT, Status=NEEDS_PLAN_APPROVAL`
- **強制停下**：回報計畫概要，等用戶說「OK / 通過 / 開始 / 確認」之類的明確指示

### CONSTRUCT（實作）

- **行為**：逐項實作 Plan，每項完成立即在 checklist 打勾並追加證據
- **限**：
  - 嚴格按 Plan，不發明 Plan 外的功能
  - 不引入未在 Plan 中核准的依賴
  - 套用 `cursor-rules/` 的代碼風格（語言別專用規則會自動觸發）
  - 每項完成後跑該檔案的 lint / build 確認沒破壞
- **發現 Plan 有錯**：
  - 立即停止實作
  - 設 `Phase=BLUEPRINT, Status=NEEDS_PLAN_APPROVAL`
  - 在 Log append：`[time] CONSTRUCT → BLUEPRINT | 原因：...`
  - 提出修訂版 Plan 等用戶確認
- **完成所有 Plan 項目**：自動進 VALIDATE

### VALIDATE（驗證）

- **行為**：跑完整 build + lint + test
- **失敗分類**（對齊 `cursor-agents/test-writer.md`）：
  - Production code bug → 回 CONSTRUCT 修
  - Test 自己的問題 → 直接修，不算失敗輪次
  - 既有失敗（與本次改動無關）→ 標記不算失敗輪次
- **失敗處理**：
  - `ValidateAttempts += 1`，記錄到 Log
  - 若 < 3 → 設 `Phase=CONSTRUCT, Status=IN_PROGRESS`，回去修
  - 若 = 3 → 設 `Phase=BLOCKED, Status=BLOCKED`，停下等人介入
- **通過**：自動進 DELIVER

### DELIVER（交付 review）

- **行為**：五層 review（對齊 `cursor-agents/code-reviewer.md`）
  1. Spec / Plan 一致性
  2. 架構一致性
  3. 正確性（邏輯、邊界、error handling）
  4. 品質（命名、複雜度、測試覆蓋）
  5. 安全性（輸入驗證、敏感資料、依賴風險）
- **產**：Review 摘要，每個 finding 分 Blocker / Major / Minor，附檔案行號
- **完成**：設 `Phase=DELIVER, Status=NEEDS_HUMAN_REVIEW`
- **強制停下**：回報變更清單 + 測試結果 + Review findings，等用戶測試確認，**不可自動 commit**

### BLOCKED

- 回報目前卡點、嘗試過的方法、需要用戶決策的問題
- 等用戶把 Status 改回 `IN_PROGRESS` 或調整 Plan 後才能繼續

## Spec-driven 變體

當任務描述含 spec / architecture 文件路徑（如 `docs/specs/auth.md`、`docs/architecture/`、用戶口頭說「依這份 spec 實作」），啟用此變體。
它**不取代**五階段流程，只調整每階段的輸入與委派策略。

### 觸發判定

任一條件成立即啟用：

- CurrentTask 含 `docs/specs/` 或 `docs/architecture/` 路徑
- CurrentTask 含「實作 spec / 依 spec / 依架構文件 / 執行 architecture」等字眼
- 用戶在對話中明確提供 spec 文件路徑

找不到指定的 spec 文件 → **不要自己發明路徑**，停下問用戶。

### 各階段調整

| Phase | 預設行為 | Spec-driven 變體 |
|---|---|---|
| ANALYZE | 自行分析任務 | 以 spec 為主要輸入；不重新分析需求；只摘要 spec 範圍與依賴 |
| BLUEPRINT | 從理解推導 checklist | 直接從 spec 的實作順序推導 checklist；scope 嚴格對齊 spec |
| CONSTRUCT | 主 agent 自行實作 | 若 `cursor-agents/code-coder.md` 存在 → 委派 `@code-coder`；否則自行實作但遵守 code-coder 規則（嚴格遵守 spec、不重構 scope 外） |
| VALIDATE | 主 agent 跑 build + lint + test | 若 `cursor-agents/test-writer.md` 存在 → 委派 `@test-writer` 補測試 + 跑套件；否則照舊 |
| DELIVER | 主 agent 做五層 review | 若 `cursor-agents/code-reviewer.md` 存在 → 委派 `@code-reviewer`（readonly 強制）；否則照舊 |

### 委派時的補充規則

- **委派只發生在 CONSTRUCT / VALIDATE / DELIVER**，ANALYZE 與 BLUEPRINT 永遠由主 agent 處理（要與用戶互動確認）
- **委派前必須附帶輸入**（subagent 上下文是隔離的，看不到完整對話）：
  - `@code-coder`：spec 路徑 / BLUEPRINT plan 條目 / 明確需求描述（三選一）
  - `@test-writer`：改動範圍 / 指定測試目標 / 上一階段 code-coder 的完成回報（三選一）
  - `@code-reviewer`：git diff / 檔案清單 / 上一階段的完成回報（三選一）
  - 三者都沒有時 subagent 會拒絕執行（其 frontmatter 內含 Pre-flight 自我檢查），主 agent 應在委派前先補齊
- 委派 subagent 後，主 agent 仍負責更新 `workflow-state.md`（subagent 不寫 state）
- subagent 失敗 3 次 → 升級為 BLOCKED，**不要繼續硬刷**
- 委派事件記入 Log：`[time] CONSTRUCT 模組 X 委派 @code-coder`

### 反模式（變體專屬）

- **禁止** spec 沒提到就自己加功能（code-coder 規則：spec 沒說的就停下問）
- **禁止** 為了符合 spec 而擅改既有不相關代碼
- **禁止** code-reviewer subagent 修檔（readonly 強制）
- **禁止** 找不到 spec 卻憑記憶或臆測「應該長這樣」開始實作

## 模型選擇建議（非強制）

模型分配影響成本、速度、品質。以下為「組合 A：CP 值最佳」推薦，適合日常 agentic 開發：

| 角色 | 推薦模型 | 理由 |
|---|---|---|
| 主 agent（執行此 rule） | `Composer-2` | Cursor 自家、最快、最便宜，擅長協調委派 |
| `@code-coder` | `gpt-5.3-codex` | 為 agentic coding 訓練，寫代碼專精 |
| `@test-writer` | `claude-4.6-sonnet` | 平衡穩定，理解測試框架 |
| `@code-reviewer` | `claude-4.7-opus-thinking` | 深度推理找盲點 |

- 主 agent 在 Cursor IDE 右下角 model picker 切換
- subagent 在 `cursor-agents/*.md` 的 frontmatter `model:` 欄位設定

### 階段性評估（每 1-2 週回顧）

| 觀察到的問題 | 調整方向 |
|---|---|
| 主 agent 規劃方向常偏 / 拆 plan 不夠細 | 主 agent 升 `gpt-5.5-medium` 或 `claude-4.7-opus-thinking` |
| code-coder 實作品質不穩 / 偏離 spec | 改 `claude-4.6-sonnet` 或升 `claude-4.7-opus-thinking` |
| code-reviewer false positive 太多 / 太雜 | 改 `gpt-5.3-codex` 或 `claude-4.6-sonnet` |
| 成本壓力大 / 想跑大量並行 | 全部降階為 `composer-2-fast` |

### 平台限制

- **Usage-based plan**（Pro / Pro Plus / Ultra）：subagent `model:` 設定會被正常 honor，**無 Max Mode 限制**
- **Legacy request-based plan**：GPT-5.3 Codex / Claude 4.6+ Sonnet / Claude 4.6+ Opus 需要 Max Mode，否則 subagent 會 fallback 到 Composer
- 不確定自己是哪種 plan → Cursor Settings → Billing 查看

## 回報格式

每次階段轉換時的回報模板：

```
✅ Phase X 完成 → Phase Y
- 改動：[檔案 + 行數]
- 驗證：[build/test 結果]
- 下一步：[等用戶確認 / 自動進入 Y]
```

進度顯示用：

```
✅ ANALYZE
✅ BLUEPRINT（已確認）
🔄 CONSTRUCT 3/7
⏸️ VALIDATE
⏸️ DELIVER
```

## 與既有 rules 的協作

- **`workflow-dev-plan.mdc`**：BLUEPRINT 即計畫階段，滿足「開發前必須有計畫」
- **`workflow-git-commit.mdc`**：DELIVER 後等用戶確認才 commit；本規則不取代 git commit 流程
- **`workflow-completion-evidence.mdc`**：每階段轉換的回報自動含「改了什麼 / 怎麼驗證 / 下一步」三要素
- **`workflow-language.mdc`**：所有回報用繁體中文
- **`general-comments.mdc`**、**`general-security.mdc`**：CONSTRUCT 階段套用

## 反模式

- **禁止** Status 是 `NEEDS_*` 時還繼續行動
- **禁止** CONSTRUCT 中改 Plan 卻不退回 BLUEPRINT
- **禁止** VALIDATE 沒過就跳 DELIVER
- **禁止** DELIVER 後自動 commit
- **禁止** 把 IN_PROGRESS 寫成完成卻沒附驗證證據
