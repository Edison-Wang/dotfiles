# decisions/

Architecture Decision Records（ADR），記錄本 repo 自身的設計決策。

專案級的 ADR 放在各專案自己的 `decisions/` 目錄。

## 命名

`<YYYY-MM-DD>-<簡短名稱>.md`

## 範本

見 `_template.md`。

## 狀態流轉

- **提案中** — 已寫但尚未驗證效果
- **已接受** — 跑過一段時間驗證 OK
- **已棄用** — 不再採用
- **被 ADR-XXX 取代** — 被新決策取代
